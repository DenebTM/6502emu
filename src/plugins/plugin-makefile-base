CXX = g++
CXXFLAGS = -g -std=c++20 -fPIC

INCLUDE = ../../include

$(OUTFILE): $(OBJS)
	$(CXX) -shared -o $@ $^ $(LDLIBS)

%.o: %.cpp
	$(CXX) -I$(INCLUDE) $(CXXFLAGS) -c $<

clean:
	$(RM) $(OUTFILE) $(OBJS)
