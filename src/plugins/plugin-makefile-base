CXX = g++
CXXFLAGS = -g -std=c++20 -fPIC -MMD

INCLUDE = ../../include

$(OUTFILE): $(OBJS)
	$(CXX) -shared -o $@ $^ $(LDLIBS)

-include $(OBJS:%.o=%.d)

%.o: %.cpp
	$(CXX) -I$(INCLUDE) $(CXXFLAGS) -c $<

clean:
	$(RM) $(OUTFILE) $(OBJS) $(OBJS:.o=.d)
