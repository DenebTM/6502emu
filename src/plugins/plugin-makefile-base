CXX = g++
CXXFLAGS = -g -Wall -Wpedantic -std=c++20 -fPIC -fvisibility=hidden -MMD

INCLUDE = ../../include

$(OUTFILE): $(OBJS)
	$(CXX) -shared -o $@ $^ $(LDLIBS)

-include $(OBJS:%.o=%.d)

%.o: %.cpp
	$(CXX) -I$(INCLUDE) $(CXXFLAGS) -c $<

clean:
	$(RM) $(OUTFILE) $(OBJS) $(OBJS:.o=.d)
